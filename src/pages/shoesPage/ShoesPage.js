import React from 'react';
import { withRouter } from 'react-router-dom';

const ShoesPage = () => {
    return (
        <div>
            <h1>Shoes Page</h1>
        </div>
    );
};

export default withRouter(ShoesPage);
